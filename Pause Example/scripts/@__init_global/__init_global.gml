gml_pragma( "global", "__init_global();");

// définir des valeurs globales par défaut

layer_force_draw_depth(true,0);		// forcer toutes les couches à dessiner à la profondeur 0
draw_set_colour( c_black );