
/// @ description Description Détermine si un arrière-plan existe ou non.
/// @param index L'index de l'arrière-plan à vérifier.
/// @returns {boolean} si l'arrière-plan existe ou non

return sprite_exists(argument0);